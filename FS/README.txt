All files in this directory should be written in or with F# 3.1.x in mind.

To compile `.fs` files you will need Mono and will run:
"fsharpc file.fs"

To run compiled `.exe` files, run:
`mono file.exe`

